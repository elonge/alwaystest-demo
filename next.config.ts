import type { NextConfig } from "next";

const nextConfig: NextConfig = {
      devIndicators: {
        buildActivity: false,
      },

  /* config options here */
};

export default nextConfig;

// optimized for fast purchase decisions - add a buy now button


